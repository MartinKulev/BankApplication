@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<!DOCTYPE html>
<html>
<head>
	<title>You have succesfully taken a credit!</title>
	<style>
		body {
			background-color: #f2f2f2;
			font-family: Arial, sans-serif;
		}

		.success-box {
			background-color: white;
			border: 2px solid green;
			color: green;
			font-size: 1.5em;
			padding: 20px;
			text-align: center;
			position: absolute;
			top: 50%;
			left: 50%;
			transform: translate(-50%, -50%);
			border-radius: 10px;
			box-shadow: 0px 0px 10px 0px rgba(0,0,0,0.3);
		}

		.button {
			background-color: #4CAF50;
			color: white;
			padding: 10px 20px;
			border: none;
			border-radius: 5px;
			cursor: pointer;
			font-size: 1.2em;
			transition: background-color 0.3s ease-in-out;
			position: absolute;
			top: 70%;
			left: 50%;
			transform: translate(-50%, -50%);
		}

			.button:hover {
				background-color: #3e8e41;
			}
	</style>
</head>
<body>
	<div class="success-box">
		<p>You have succesfully taken a credit!</p>
		@if (ViewData["CreditSuccessfulyTakenMessage"] != null)
		{
			<p><span style="color:green;">@ViewData["CreditSuccessfulyTakenMessage"]</span></p>
		}
	</div>
	<form method="post" asp-controller="Bank" asp-action="SuccesfulTakenCredit">
		<button type="submit" class="button">Go Back <input hidden value="goBackButton" name="optionButton" /> </button>
	</form>
</body>
</html>

